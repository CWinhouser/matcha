{{#each messages as | message |}}
        <div class="row">
            <div class="col-md-9">
                <div class="row">
                    <div class="col-md-4">
                        <span>Notification: {{ notification }}</span>
                    </div>
                </div>
                <br />
            </div>
            <hr />
        </div>
        {{/each}}